You are an expert C/C++ security engineer.

Generate a minimal, compilable LibFuzzer fuzz driver for the target C/C++ API below.

Requirements:
- Output only C++ source code.
- Always include <cstdint> and <cstddef> for uint8_t and size_t.
- Include the provided header.
- Define exactly one entry point:
  extern "C" int LLVMFuzzerTestOneInput(const uint8_t* data, size_t size)
- Treat fuzzer input as arbitrary bytes.
- If size is 0, return 0 early.
- Ensure any string passed to APIs expecting const char* is null-terminated (copy data into a buffer with a trailing '\0').
- For functions with multiple parameters, split the fuzz data into segments for each parameter. Use size checks to ensure enough data is available before proceeding.
- If the target API returns an object or allocation, release it with the cleanup function.
- Keep the driver simple and deterministic.
- Do not read files, use networking, sleep, or spawn processes.
- Do not add exploit logic or attack logic.
- The driver must compile with the listed implementation source files and include directories.

Example output for a JSON parser with signature `cJSON *cJSON_Parse(const char *value)`:
```cpp
#include <cstdint>
#include <cstddef>
#include <cstring>
#include "cJSON.h"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *data, size_t size) {
    if (size == 0) return 0;
    char *str = new char[size + 1];
    memcpy(str, data, size);
    str[size] = '\0';
    cJSON *obj = cJSON_Parse(str);
    if (obj) cJSON_Delete(obj);
    delete[] str;
    return 0;
}
```

Target name:
{{TARGET_NAME}}

Language:
{{LANGUAGE}}

Function name:
{{FUNCTION_NAME}}

Function signature:
{{FUNCTION_SIGNATURE}}

Header to include:
{{HEADER}}

Target function notes:
{{TARGET_DESCRIPTION}}

Cleanup function:
{{CLEANUP_FUNCTION}}

Implementation source files:
{{SOURCE_FILES}}

Include directories:
{{INCLUDE_DIRS}}

Seed corpus:
{{SEED_CORPUS}}
