You are a security researcher designing a fuzz driver for the {{LIBRARY_NAME}} library ({{LANGUAGE}}).
Description: {{DESCRIPTION}}

Public API signatures from the header:
{{SOURCE_CODE}}

Provide a concise analysis:

1. API INVENTORY: List key public functions by category (parse/create/modify/query/delete/serialize).

2. CALL SEQUENCES: Show 2-3 typical multi-API call chains (e.g., parse->query->delete).

3. MEMORY PATTERNS: Which functions allocate, and what frees them.

4. FUZZING STRATEGY: Design 2 approaches to maximize branch coverage:
- Approach A: Feed raw fuzz bytes to parsing APIs, exercise downstream APIs on result.
- Approach B: Use fuzz bytes to drive construction APIs, then serialize/query.
For each: list API call order, how to interpret fuzz input bytes, cleanup needed.

5. HARD-TO-REACH PATHS: List 3-5 specific branches that need special input patterns.

Be concise. No boilerplate.
